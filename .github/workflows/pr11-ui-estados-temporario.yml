name: PR11 UI Estados Temporario Desativado

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow temporario desativado; remover antes do merge."
